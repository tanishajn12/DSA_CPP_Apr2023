#include<bits/stdc++.h>
using namespace std;

void help(string input, string output, vector<string>& s) {
    if(input.size()==0) {
        s.push_back(output);
        return;
    }

    if(input[0]=="0") {
        return; //ignore the output coming from zero
    }

    
}